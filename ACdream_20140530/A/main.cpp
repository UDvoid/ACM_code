#include <iostream>
#include <cstdio>
using namespace std;
int T, n, m;
int ans;
int flag;
void solve(int k)
{
    int i;
    if(k > n)
    {
        flag = 1;
        for(i = 0; i < n; i++)
            if(a[i] != 0)
                ans *= a[i];
        return;
    }
    for(i = 0; i < )
    int i, j;

}
int main()
{
    scanf("%d", &T);
    while(T--)
    {
        ans = 1;
        flag = 0;
        memset(a, 0, sizeof(a));
        scanf("%d %d",&n, &m);

    }
    return 0;
}
